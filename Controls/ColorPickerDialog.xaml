<Window x:Class="WebWeaver.Controls.ColorPickerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Выбрать цвет" Width="400" Height="350"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource Brush.PanelBg}"
        Foreground="{DynamicResource Brush.TextBoxFg}"
        ResizeMode="NoResize" ShowInTaskbar="False">

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Выбрать цвет или ввести hex код"
                   FontSize="12" Foreground="{DynamicResource Brush.LabelFg}" Margin="0,0,0,12"/>

        <!-- Недавние цвета: в шаблоне поменялся только BorderBrush -->
        <ItemsControl Grid.Row="1" x:Name="recentColorsGrid" Margin="0,0,0,8">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Width="24" Height="24" Margin="4"
                            Background="{Binding HexCode}"
                            BorderBrush="{DynamicResource Brush.BtnBorder}" BorderThickness="1"
                            Cursor="Hand" ToolTip="{Binding HexCode}"
                            MouseLeftButtonDown="RecentColor_MouseLeftButtonDown"
                            Tag="{Binding HexCode}"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- Hex -->
        <Grid Grid.Row="2" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="60"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Hex:"
                       Foreground="{DynamicResource Brush.LabelFg}"
                       VerticalAlignment="Center" Margin="0,0,8,0"/>
            <TextBox x:Name="txHexCode" Grid.Column="1"
                     Background="{DynamicResource Brush.TextBoxBg}"
                     Foreground="{DynamicResource Brush.TextBoxFg}"
                     CaretBrush="{DynamicResource Brush.TextBoxFg}"
                     BorderBrush="{DynamicResource Brush.AccentBtn}" BorderThickness="1"
                     Padding="8,6" VerticalAlignment="Center"
                     TextChanged="TxHexCode_TextChanged"/>
            <Border x:Name="prevColor" Grid.Column="2"
                    Background="White"
                    BorderBrush="{DynamicResource Brush.BtnBorder}" BorderThickness="1"
                    Margin="8,0,0,0"/>
        </Grid>

        <!-- Палитра: в шаблоне тоже BorderBrush -->
        <ItemsControl Grid.Row="3" x:Name="colorGrid" Margin="0,0,0,12">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Width="32" Height="32" Margin="4"
                            Background="{Binding HexCode}"
                            BorderBrush="{DynamicResource Brush.BtnBorder}" BorderThickness="1"
                            Cursor="Hand" ToolTip="{Binding HexCode}"
                            MouseLeftButtonDown="ColorBorder_MouseLeftButtonDown"
                            Tag="{Binding HexCode}"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <Separator Grid.Row="3" Background="{DynamicResource Brush.BtnBorder}" Margin="0,0,0,12"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="btnCancel" Content="Отмена" Width="80" Height="32"
                    Background="{DynamicResource Brush.BtnBg}"
                    Foreground="{DynamicResource Brush.BtnText}"
                    BorderThickness="0" Margin="0,0,8,0"
                    Click="BtnCancel_Click" Cursor="Hand"/>
            <Button x:Name="btnOk" Content="✓ OK" Width="80" Height="32"
                    Background="{DynamicResource Brush.AccentBtn}"
                    Foreground="{DynamicResource Brush.BtnText}"
                    BorderThickness="0" Click="BtnOk_Click" Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Window>